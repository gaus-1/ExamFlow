# 🏗️ Архитектура проекта ExamFlow

## 📋 Принципы проектирования (PEP20 - Zen of Python)

Проект следует принципам Python:

- **Beautiful is better than ugly** - Код читаемый и понятный
- **Simple is better than complex** - Простые решения предпочтительнее
- **Readability counts** - Читаемость важна
- **There should be one-- and preferably only one --obvious way to do it** - Один очевидный способ
- **Flat is better than nested** - Плоская структура лучше вложенной
- **Sparse is better than dense** - Разреженный код лучше плотного

## 📁 Структура проекта

```
ExamFlow/
├── examflow_project/      # Конфигурация Django проекта
│   ├── settings.py        # Основные настройки
│   ├── settings_components/  # Модульные компоненты настроек
│   ├── urls.py            # Главные URL маршруты
│   └── wsgi.py / asgi.py  # WSGI/ASGI конфигурация
│
├── core/                  # Основное приложение
│   ├── models.py          # Модели данных
│   ├── views.py           # Представления
│   ├── services/          # Бизнес-логика (сервисы)
│   ├── rag_system/        # RAG система для AI
│   ├── personalization/   # Система персонализации
│   └── management/        # Django management команды
│
├── learning/              # Модуль обучения
│   ├── models.py          # Модели предметов и заданий
│   ├── views.py           # Представления
│   └── urls.py            # URL маршруты
│
├── ai/                    # Модуль AI помощника
│   ├── services.py        # AI сервисы
│   ├── models.py          # Модели AI
│   └── urls.py            # URL маршруты
│
├── telegram_bot/          # Telegram бот
│   ├── bot_handlers.py    # Обработчики команд
│   ├── commands/          # Команды бота
│   ├── services/          # Сервисы бота
│   ├── utils/             # Утилиты
│   └── formatters/        # Форматирование сообщений
│
├── telegram_auth/         # Telegram аутентификация
│   ├── models.py          # Модели пользователей
│   ├── services.py        # Сервисы аутентификации
│   └── views.py           # Представления
│
├── analytics/             # Аналитика
├── themes/                # Система тем
├── tests/                 # Тесты
├── static/                # Статические файлы
├── templates/             # HTML шаблоны
└── docs/                  # Документация
```

## 🎯 Принципы организации кода

### 1. Разделение ответственности (SRP)

Каждый модуль должен иметь одну ответственность:

- **Models** - только структура данных
- **Views** - только обработка HTTP запросов
- **Services** - только бизнес-логика
- **Utils** - только вспомогательные функции

### 2. Импорты (PEP8)

Правильный порядок импортов:

```python
# 1. Стандартная библиотека
import logging
from typing import Any

# 2. Сторонние библиотеки
from django.conf import settings
from django.db import transaction

# 3. Локальные импорты
from .models import User
from .services import UserService
```

### 3. Именование (PEP8)

- **Функции и переменные**: `snake_case`
- **Классы**: `CamelCase`
- **Константы**: `UPPER_SNAKE_CASE`
- **Приватные методы**: `_leading_underscore`
- **Приватные атрибуты**: `_leading_underscore`

### 4. Длина строки (PEP8)

- Максимум 88 символов (Black default)
- Перенос строк с правильными отступами
- Использование скобок для длинных выражений

### 5. Докстринги (PEP257/Google)

```python
def function_name(param1: str, param2: int) -> bool:
    """Краткое описание функции.

    Подробное описание, если нужно.

    Args:
        param1: Описание параметра 1
        param2: Описание параметра 2

    Returns:
        Описание возвращаемого значения

    Raises:
        ValueError: Когда возникает ошибка
    """
    pass
```

### 6. Обработка ошибок

```python
# ✅ Хорошо - конкретные исключения
try:
    result = process_data()
except ValueError as e:
    logger.error(f"Ошибка валидации: {e}")
    return None

# ❌ Плохо - bare except
try:
    result = process_data()
except:
    pass
```

### 7. Type Hints

Все публичные функции должны иметь аннотации типов:

```python
def process_user(user: User, settings: dict[str, Any]) -> bool:
    """Обрабатывает пользователя."""
    pass
```

## 🏛️ Архитектурные паттерны

### Dependency Injection

Используется `core.container.Container` для внедрения зависимостей:

```python
from core.container import Container

container = Container()
service = container.get_service('user_service')
```

### Service Layer

Бизнес-логика в сервисах, не в views:

```python
# ✅ Хорошо
class UserService:
    def create_user(self, data: dict) -> User:
        # Бизнес-логика здесь
        pass

# ❌ Плохо
def create_user_view(request):
    # Бизнес-логика в view
    pass
```

### Repository Pattern (где применимо)

Инкапсуляция доступа к данным:

```python
class TaskRepository:
    def get_by_subject(self, subject_id: int) -> list[Task]:
        return Task.objects.filter(subject_id=subject_id)
```

## 🔄 Планы улучшения архитектуры

### Фаза 1: Стандартизация (в процессе)
- ✅ Правильная организация импортов
- ✅ Type hints для публичных API
- ✅ Докстринги для всех публичных функций
- ✅ Соответствие PEP8

### Фаза 2: Рефакторинг (запланировано)
- 🔄 Декомпозиция монолитных модулей
- 🔄 Устранение дублирования кода
- 🔄 Улучшение структуры модулей

### Фаза 3: Оптимизация (запланировано)
- 🔄 Производительность критических путей
- 🔄 Оптимизация запросов к БД
- 🔄 Кэширование стратегий

## 📚 Дополнительные ресурсы

- [PEP 8 - Style Guide](https://pep8.org/)
- [PEP 20 - Zen of Python](https://www.python.org/dev/peps/pep-0020/)
- [PEP 257 - Docstring Conventions](https://www.python.org/dev/peps/pep-0257/)
- [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html)

